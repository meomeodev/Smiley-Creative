const fs = require('fs'); let raw = fs.readFileSync('raw_body.html', 'utf8'); let idx = fs.readFileSync('index.html', 'utf8'); const startRaw = raw.indexOf('data-id="94d8014"'); const endRaw = raw.indexOf('data-id="98397b4"'); const startIdx = idx.indexOf('data-id="94d8014"'); const endIdx = idx.indexOf('data-id="98397b4"'); if (startRaw > -1 && startIdx > -1) { let newIdx = idx.substring(0, startIdx) + raw.substring(startRaw, endRaw) + idx.substring(endIdx); fs.writeFileSync('index.html', newIdx); console.log('Successfully copied gallery from raw_body to index.'); } else { console.log('Could not find gallery ids'); }
